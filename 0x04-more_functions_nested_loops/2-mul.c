#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first number to multiply
 * @b: second number to multiply with first number
 * Return: result of multiplication between the two parameters
 *
 */

int mul(int a, int b)
{
	return (a * b);
}
